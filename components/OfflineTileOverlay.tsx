/**
 * Type stub for TypeScript — Metro ignores this file at runtime and
 * uses OfflineTileOverlay.native.tsx (Android/iOS) or OfflineTileOverlay.web.tsx (browser).
 * This file exists only so the TS compiler can resolve the import.
 */
export { default } from './OfflineTileOverlay.native';
